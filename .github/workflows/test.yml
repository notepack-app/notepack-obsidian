name: Tests

permissions:
  contents: read

on:
  workflow_call:
  workflow_dispatch:
  pull_request:
    branches:
      - dev
  push:
    branches:
      - dev
    paths-ignore:
      - '*.md'
      - 'scripts/**'
      - '.claude/**'

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test
